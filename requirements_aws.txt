# AWS CryptoPulse Requirements
Flask==2.3.3
Werkzeug==2.3.7
requests==2.31.0

# AWS SDK
boto3==1.34.0
botocore==1.34.0

# Additional dependencies for AWS deployment
gunicorn==21.2.0
python-dotenv==1.0.0

# Chart.js and frontend dependencies (served via CDN)
# Bootstrap 5.3.0 (served via CDN)
# Font Awesome 6.4.0 (served via CDN)